<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.nuxeo</groupId>
    <artifactId>nuxeo-parent</artifactId>
    <version>2025.24</version>
  </parent>

  <groupId>org.nuxeo.coldstorage</groupId>
  <artifactId>nuxeo-coldstorage-parent</artifactId>
  <name>Nuxeo Cold Storage Parent</name>
  <version>2025.3.26</version>
  <packaging>pom</packaging>
  <description>Nuxeo Cold Storage allows the cold storage of document content. This can be needed for archiving,
    compliance, etc.
  </description>

  <properties>
    <frontend-plugin.node.version>v22.17.0</frontend-plugin.node.version>
    <frontend-plugin.node.download.root/>
    <frontend-plugin.node.server.id/>
    <frontend-plugin.node.npm.userconfig>${env.HOME}/.npmrc</frontend-plugin.node.npm.userconfig>
    <frontend-plugin.ftest.nuxeoUrl>http://localhost:8080</frontend-plugin.ftest.nuxeoUrl>
  </properties>

  <modules>
    <module>nuxeo-coldstorage</module>
    <module>nuxeo-coldstorage-package</module>
    <module>nuxeo-coldstorage-web</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.nuxeo.coldstorage</groupId>
        <artifactId>nuxeo-coldstorage</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.nuxeo.coldstorage</groupId>
        <artifactId>nuxeo-coldstorage-package</artifactId>
        <version>${project.version}</version>
        <type>zip</type>
      </dependency>
      <dependency>
        <groupId>org.nuxeo.coldstorage</groupId>
        <artifactId>nuxeo-coldstorage-web</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <repositories>
    <repository>
      <id>maven-public-lts</id>
      <url>https://packages.nuxeo.com/repository/maven-public-lts</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <updatePolicy>always</updatePolicy>
        <enabled>true</enabled>
      </snapshots>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>maven-public-lts</id>
      <url>https://packages.nuxeo.com/repository/maven-public-lts</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <!-- no need to enable snapshots for plugin repository -->
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </pluginRepository>
  </pluginRepositories>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <configuration combine.children="append">
            <ratchetFrom>origin/lts-2025</ratchetFrom>
          </configuration>
        </plugin>
        <plugin>
          <groupId>com.github.eirslett</groupId>
          <artifactId>frontend-maven-plugin</artifactId>
          <version>1.15.4</version>
          <configuration>
            <nodeVersion>${frontend-plugin.node.version}</nodeVersion>
            <nodeDownloadRoot>${frontend-plugin.node.download.root}</nodeDownloadRoot>
            <serverId>${frontend-plugin.node.server.id}</serverId>
            <environmentVariables>
              <NPM_CONFIG_USERCONFIG>${frontend-plugin.node.npm.userconfig}</NPM_CONFIG_USERCONFIG>
            </environmentVariables>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <scm>
    <connection>scm:git:git://github.com/nuxeo/nuxeo-coldstorage.git</connection>
    <developerConnection>scm:git:ssh://git@github.com:nuxeo/nuxeo-coldstorage.git</developerConnection>
    <url>https://github.com/nuxeo/nuxeo-coldstorage</url>
  </scm>

</project>
